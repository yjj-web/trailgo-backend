{
  "name": "trailgo-backend",
  "version": "1.0.0",
  "description": "野径 TrailGo 后端服务",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node initDb.js",
    "deploy": "sls deploy",
    "package": "powershell -Command \"if (Test-Path trailgo-backend.zip) { Remove-Item trailgo-backend.zip }; Compress-Archive -Path 'server.js', 'package.json', 'trailgo.db', 'scf_bootstrap', 'node_modules' -DestinationPath 'trailgo-backend.zip' -Force; Write-Host '打包完成！'\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serverless-http": "^3.2.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "serverless": "^3.38.0"
  }
}
